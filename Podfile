source 'https://git.coding.net/PandaCashBoxiOS/PandaSpec.git'
source 'https://github.com/CocoaPods/Specs.git'

project 'GestureConflict.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GestureConflict' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  	pod 'PhotoBrowser', '~> 1.0.6'

  target 'GestureConflictTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GestureConflictUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
